[project]
name = "openkoutsi-docs"
version = "0.1.0"
description = "User-facing documentation for openkoutsi"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.5",
    "mkdocs-static-i18n>=1.2",
]
